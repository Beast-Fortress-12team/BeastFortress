using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("이거 확인용 추가추가추가");  
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
